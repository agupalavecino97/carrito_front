export class Producto {
    constructor(id: string,codigo: string,nombre: string, cantidad: number){
        this.id = id,
        this.codigo = codigo,
        this.nombre = nombre,
        this.cantidad = cantidad
    }
    id: string;
    codigo: string;
    nombre: string;
    cantidad: number;

}
